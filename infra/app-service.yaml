apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: default
  labels:
    app: cicd
spec:
  type: LoadBalancer
  selector:
    app: cicd
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
