terraform apply --auto-approve
eksctl create cluster -f cluster.yaml
docker login
kubectl create secret generic regcred --from-file=.dockerconfigjson=/home/denis/.docker/config.json --type=kubernetes.io/dockerconfigjson
kubectl apply -f deploy.yaml
kubectl apply -f crd.yaml
kubectl create namespace argocd
kubectl create secret generic regcred --from-file=.dockerconfigjson=/home/denis/.docker/config.json --type=kubernetes.io/dockerconfigjson -n argocd
kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
kubectl patch svc argocd-server -n argocd -p '{"spec": {"type": "LoadBalancer"}}'
curl -sSL -o argocd https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
chmod +x argocd
sudo mv argocd /usr/local/bin/
argocd admin initial-password -n argocd
# give argocd access to github repo
kubectl apply -f application.yaml
